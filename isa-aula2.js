const forma = 'quadrado';
const lado = 6;
const lado2 = 7;
const area = lado ** 2;
const areaAlgo = lado * lado2;
if (forma === 'quadrado'){
    console.log(area);
} else {
    console.log(areaAlgo);
}
console.log (`a area do quadrado eh ${area}`);
